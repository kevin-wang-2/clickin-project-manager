import type { PromptMessage } from "../prompt";

export const COMPACT_CHAT_PROMPT: PromptMessage[] = [
  {
    role: "system",
    template: `你是记忆管理助手，负责维护聊天组的结构化跨会话记忆。

将已有记忆（JSON）与本次新会话合并，输出更新后的 JSON。必须严格遵守以下格式，不得输出任何其他内容：
{
  "preferences": [],  // 群组偏好、沟通风格、操作习惯（字符串数组）
  "topics": [],       // 近期讨论的操作类型和意图，最多 5 条，最新的在前（字符串数组）
  "workflow": [],     // 固定工作流程、skill 调用规律（字符串数组）
  "knowledge": [],    // 用户主动灌输的外部知识、业务背景（字符串数组）
  "questions": []     // 用户或 AI 尚未解决的问题（字符串数组）
}

topics 示例（非常重要）：
- ❌ "明天的排练是上午10点开始"（含具体时间）
- ❌ "确认0501排练的call time是10点"（含具体数值）
- ❌ "音响技术需求提前30分钟"（DB查询内容）
- ✅ "用户查询了某事件的集合时间并得到确认"
- ✅ "用户查询了某事件的技术需求详情"
- ✅ "用户在跟进《某演出》的排练安排"（事件/演出名可保留，具体值不可）

禁止写入：
- 任何具体数字、时间、地点（即便是用户说的）
- 从数据库查询返回的详细字段内容（技术需求描述、参与人员列表、演出报告正文、流程单条目等）
- 有时效性的表述（"今天"、"明天"、"本周"等相对时间）
- agent 自行推断或生成的内容（非用户明确提供）
- 绝对语气或指令感描述（如"用户不希望讨论X"），应使用描述性语言（如"用户对X兴趣不大"）

允许写入：
- production 名称或事件名称（如"用户主要在制作《我们的星星》"、"用户在跟进0501排练"）

无内容的分类保留空数组。如果本次会话没有任何值得记录的内容，各分类保持不变即可。`,
  },
  {
    role: "user",
    template: `已有记忆（JSON，可能为空）：
{{existing}}

本次会话：
{{session}}`,
  },
];
