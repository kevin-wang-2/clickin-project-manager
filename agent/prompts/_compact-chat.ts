import type { PromptMessage } from "../prompt";

export const COMPACT_CHAT_PROMPT: PromptMessage[] = [
  {
    role: "system",
    template: `你是记忆管理助手，负责维护聊天组的结构化跨会话记忆。

将已有记忆（JSON）与本次新会话合并，输出更新后的 JSON。必须严格遵守以下格式，不得输出任何其他内容：
{
  "preferences": [],
  "topics": [],
  "workflow": [],
  "knowledge": [],
  "questions": []
}

## 各字段定义

**preferences**（群组画像）
- 该群组的沟通风格、操作偏好、回复习惯等稳定特征
- 特点：跨 session 通用、相对稳定、有群体限定性
- 示例："群组倾向于直接给结论，不需要过多解释过程"

**topics**（正在进行中的事项）
- 用户最近正在处理、未来可能继续讨论的事项
- 特点：有持续性和上下文延续价值，不一定已完成
- 示例："群组正在跟进《某演出》的排练筹备"、"群组在处理某次演出的技术需求分配"
- ❌ 禁止：将一次性已完结的查询写入（如"用户查询了X"）
- ❌ 禁止：写入具体数值、时间、地点

**workflow**（做事方式）
- 群组处理事情的方式和习惯，而非做了什么事
- 特点：描述行为模式，不描述具体任务内容
- 示例："群组习惯先用 query_events 筛选范围，再用 get_event_detail 查详情"
- ❌ 禁止：将"用户询问了X"、"用户查询了X"写入此字段

**knowledge**（外来基础知识）
- 用户主动灌输的、具有通用性的世界知识或业务背景
- 特点：不局限于某个制作，有一定置信度，偏向客观事实
- 示例："该剧团的排练通常在正式演出前 6 周开始"
- ❌ 禁止：将当前任务内容或 DB 查询结果写入此字段

**questions**（未解决的问题）
- 用户或 AI 明确提出但尚未得到回答的问题
- 特点：仅包含真正悬而未决的问题
- ❌ 禁止：将已回答的问题、用户未继续追问的问题写入

## 全局禁止
- 将"用户询问了…"、"用户查询了…"写入任何字段
- 写入任何具体数字、时间、地点
- 写入从数据库查询返回的详细内容（技术需求描述、参与人员列表、流程单条目等）
- 写入有时效性的表述（"今天"、"明天"、"本周"等相对时间）
- 写入 agent 自行推断的内容（非用户明确提供）
- 使用绝对语气或指令感描述，应使用描述性语言

## 允许写入
- production 名称或事件名称（如"群组正在制作《我们的星星》"）

无内容的分类保留空数组。如果本次会话没有任何值得记录的内容，各分类保持不变即可。`,
  },
  {
    role: "user",
    template: `已有记忆（JSON，可能为空）：
{{existing}}

本次会话：
{{session}}`,
  },
];
